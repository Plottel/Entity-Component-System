using System;
namespace MyGame
{
    /// <summary>
    /// Represents an enum containing all of the different Attack Types in the program.
    /// When an attack is performed, the output depends on the Entity's Attack Type.
    /// </summary>
    public enum AttackType
    {
        Melee,
        Bow,
    }
}