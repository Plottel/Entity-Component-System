using System;

namespace MyGame
{
    /// <summary>
    /// Represents the interface implemented by all Components in the program. This interface is what
    /// powers the ECS, allowing all program data (Components) to be stored as the same type.
    /// </summary>
    public interface Component
    {
    }
}